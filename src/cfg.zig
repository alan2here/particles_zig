pub const CFG = .{
    .air_resistance = 2,
    .ground_resistance = 10,
    .draw = true,
    .gravity = 4,
    .min_iters_per_frame = 1,
    .min_iters_per_second = 200,
    .tension = 10000,
    .timescale = 1,
    .vsync = true,
};
